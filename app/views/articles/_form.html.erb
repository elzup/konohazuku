<%= simple_form_for(article, html: {class: 'form-horizontal'}) do |f| %>
    <%= f.input :title %>
    <%= f.input :message %>
    <%= f.input :email_list, as: :text %>
    <%= f.button :submit %>

<% end %>
